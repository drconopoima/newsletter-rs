application:
  port: 8000
  address: localhost
  healthcachevalidityms: 1000
  smtp:
    from: postmaster@example.org
    name: newsletter-rs
    reply_to: no-reply@example.org
    relay:
      address: smtp.server.example.org
      port: 587
      credentials:
        username: smtpusername
        password: smtppassword
database:
  host: localhost
  port: 5432
  username: postgres
  password: password
  database: newsletter
  ssl:
    tls: false
  migration:
    migrate: false
    folder: migrations
